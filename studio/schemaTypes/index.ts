import post from './post'
import video from './video'
import release from './release'
import about from './about'
import store from './store'

export const schemaTypes = [post, video, release, about, store]